// next.config.js (yoki next.config.ts agar TypeScript ishlatsangiz)
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.ibb.co"], // imgbb uchun kerakli domain
  },
};

export default nextConfig;
